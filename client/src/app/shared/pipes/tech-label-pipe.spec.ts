import { TechLabelPipe } from './tech-label-pipe';

describe('TechLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TechLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
